# Technical Debt: План улучшений по Code Quality

## 📊 Прогресс итераций

| Итерация | Задача | Статус | Дата |
|----------|--------|--------|------|
| TD-1 | Автоматизация контроля качества | ⏳ Pending | - |
| TD-2 | Type hints и статическая типизация | ⏳ Pending | - |
| TD-3 | Расширение покрытия тестами | ⏳ Pending | - |
| TD-4 | Рефакторинг: Protocol/ABC для DIP | ⏳ Pending | - |
| TD-5 | Рефакторинг: TelegramBot SRP | ⏳ Pending | - |

**Легенда статусов:**
- ⏳ Pending - не начато
- 🚧 In Progress - в работе
- ✅ Done - завершено
- ❌ Blocked - заблокировано

---

## 📋 Детальный план итераций

### Итерация TD-1: Автоматизация контроля качества
**Цель:** Внедрить автоматические инструменты проверки кода

**Обновить pyproject.toml:**
- [ ] Добавить `ruff>=0.3.0` в dev зависимости (форматтер + линтер)
- [ ] Добавить `mypy>=1.9.0` в dev зависимости (type checker)
- [ ] Добавить `pytest-cov>=4.1.0` для покрытия тестами
- [ ] Добавить `pytest-asyncio>=0.23.0` для async тестов
- [ ] Обновить версию pytest до `>=8.0.0`

**Настроить Ruff:**
- [ ] Добавить секцию `[tool.ruff]` в `pyproject.toml`
- [ ] Настроить `target-version = "py311"`
- [ ] Настроить `line-length = 100`
- [ ] Добавить правила: `["E", "F", "I", "N", "UP", "B", "C4", "SIM"]`

**Настроить Mypy:**
- [ ] Добавить секцию `[tool.mypy]` в `pyproject.toml`
- [ ] Настроить `python_version = "3.11"`
- [ ] Включить `strict = true`
- [ ] Настроить `warn_return_any = true`
- [ ] Настроить `warn_unused_configs = true`

**Настроить Pytest:**
- [ ] Добавить секцию `[tool.pytest.ini_options]`
- [ ] Настроить `asyncio_mode = "auto"`
- [ ] Настроить `testpaths = ["tests"]`

**Обновить Makefile:**
- [ ] Добавить команду `format: ruff format src tests`
- [ ] Добавить команду `lint: ruff check src tests --fix`
- [ ] Добавить команду `typecheck: mypy src tests`
- [ ] Обновить команду `test` с покрытием: `pytest tests/ -v --cov=src --cov-report=term-missing`
- [ ] Добавить команду `quality` для запуска всех проверок: `make format lint typecheck test`

**Проверка соответствия:**
- [ ] ✅ Соответствует принципам KISS из `conventions.mdc` (стандартные инструменты)
- [ ] ✅ Соответствует принципу "минимум зависимостей" из `vision.md`
- [ ] ✅ Исключены pre-commit hooks (только ручной запуск через Make)
- [ ] ✅ Интеграция не нарушает существующую архитектуру

**Тест:** Запустить `make quality` - все проверки должны пройти без критических ошибок

---

### Итерация TD-2: Type hints и статическая типизация
**Цель:** Добавить полные type hints во все модули

**config.py:**
- [ ] Добавить type hints для атрибутов класса (`str`, `int`)
- [ ] Добавить тип возврата `-> None` для `__init__`
- [ ] Добавить валидацию обязательных параметров с `ValueError`
- [ ] Добавить обработку `FileNotFoundError` для system_prompt.txt

**dialogue_manager.py:**
- [ ] Заменить `list` на `list[dict[str, str]]` для истории
- [ ] Заменить `dict` на `dict[int, list[dict[str, str]]]` для dialogues
- [ ] Добавить тип возврата для всех методов
- [ ] Уточнить типы параметров: `user_id: int`, `role: str`, `content: str`

**llm_client.py:**
- [ ] Добавить type hints для всех параметров конструктора
- [ ] Заменить `list` на `list[dict[str, str]]` для messages
- [ ] Добавить тип возврата `-> str` для `get_response`
- [ ] Добавить тип для `self.client: OpenAI`

**bot.py:**
- [ ] Добавить type hints для конструктора
- [ ] Добавить типы для всех async методов: `async def ... -> None`
- [ ] Добавить типы для параметров `Message`
- [ ] Убедиться, что все атрибуты класса типизированы

**media_processor.py (будущий):**
- [ ] Создать файл с полными type hints с самого начала
- [ ] Типизировать все методы обработки медиа

**Проверка статической типизации:**
- [ ] Запустить `make typecheck` - должно пройти без ошибок
- [ ] Исправить все найденные проблемы с типами
- [ ] Убедиться, что strict mode mypy не выдает предупреждений

**Проверка соответствия:**
- [ ] ✅ Код остается простым и понятным (`conventions.mdc`)
- [ ] ✅ Типы помогают понять API без документации (`conventions.mdc`)
- [ ] ✅ Не добавляем избыточных абстракций (`conventions.mdc`)
- [ ] ✅ Следуем принципу "1 класс = 1 файл" (`vision.md`)

**Тест:** Запустить `mypy src tests --strict` - 0 ошибок

---

### Итерация TD-3: Расширение покрытия тестами
**Цель:** Увеличить покрытие до ≥80%, добавить недостающие тесты

**Создать conftest.py с фикстурами:**
- [ ] Создать `tests/conftest.py`
- [ ] Добавить фикстуру `dialogue_manager` (DialogueManager instance)
- [ ] Добавить фикстуру `mock_llm_client` (мокированный LLMClient)
- [ ] Добавить фикстуру `config` (Config с тестовыми данными)
- [ ] Добавить фикстуру `mock_message` (мокированное Telegram Message)

**Создать test_bot.py:**
- [ ] Создать `tests/test_bot.py`
- [ ] Тест `test_bot_initialization` - проверка создания экземпляра
- [ ] Тест `test_cmd_start` - проверка команды /start
- [ ] Тест `test_cmd_help` - проверка команды /help
- [ ] Тест `test_cmd_reset` - проверка команды /reset
- [ ] Тест `test_handle_message_success` - успешная обработка сообщения
- [ ] Тест `test_handle_message_error` - обработка ошибок
- [ ] Использовать `pytest-asyncio` для async тестов

**Улучшить test_dialogue_manager.py:**
- [ ] Добавить тест `test_dialogue_manager_type_hints` - проверка типов
- [ ] Добавить тест `test_concurrent_users` - несколько пользователей одновременно
- [ ] Добавить тест `test_edge_cases_empty_content` - пустое содержимое
- [ ] Использовать фикстуры из conftest.py

**Улучшить test_llm_client.py:**
- [ ] Добавить тест `test_error_handling` - обработка ошибок API
- [ ] Добавить тест `test_empty_messages` - пустой список сообщений
- [ ] Добавить тест `test_multimodal_messages` - мультимодальные сообщения (заготовка)

**Улучшить test_config.py:**
- [ ] Добавить тест `test_config_missing_required_fields` - отсутствие обязательных полей
- [ ] Добавить тест `test_config_system_prompt_file_not_found` - отсутствие файла промпта
- [ ] Добавить тест `test_config_validates_types` - проверка типов

**Проверка покрытия:**
- [ ] Запустить `pytest --cov=src --cov-report=html`
- [ ] Проверить coverage report - цель ≥80%
- [ ] Добавить недостающие тесты для некритичных участков

**Проверка соответствия:**
- [ ] ✅ Тесты простые и понятные (`conventions.mdc`)
- [ ] ✅ Тесты проверяют базовую функциональность (`vision.md`)
- [ ] ✅ Не тестируем интеграции с внешними API в unit-тестах (`vision.md`)
- [ ] ✅ Используем моки для внешних зависимостей (`vision.md`)

**Тест:** Запустить `make test` - все тесты проходят, покрытие ≥80%

---

### Итерация TD-4: Рефакторинг: Protocol/ABC для Dependency Inversion
**Цель:** Внедрить абстракции для ключевых зависимостей (SOLID DIP)

**Создать src/bot/interfaces.py:**
- [ ] Создать файл `src/bot/interfaces.py`
- [ ] Определить `Protocol` класс `LLMProvider` с методом `get_response`
- [ ] Определить `Protocol` класс `DialogueStorage` с методами для работы с историей
- [ ] Добавить docstrings с описанием контрактов
- [ ] Добавить полные type hints

**LLMProvider Protocol:**
```python
from typing import Protocol

class LLMProvider(Protocol):
    """Контракт для провайдеров LLM"""
    
    def get_response(self, messages: list[dict[str, str]]) -> str:
        """Получить ответ от LLM на основе истории сообщений"""
        ...
```

**DialogueStorage Protocol:**
```python
class DialogueStorage(Protocol):
    """Контракт для хранилищ диалогов"""
    
    def add_message(self, user_id: int, role: str, content: str) -> None:
        """Добавить сообщение в историю"""
        ...
    
    def get_history(self, user_id: int) -> list[dict[str, str]]:
        """Получить историю диалога пользователя"""
        ...
    
    def clear_history(self, user_id: int) -> None:
        """Очистить историю диалога"""
        ...
```

**Обновить bot.py:**
- [ ] Изменить тип параметров конструктора на Protocol типы
- [ ] `llm_client: LLMProvider` вместо `LLMClient`
- [ ] `dialogue_manager: DialogueStorage` вместо `DialogueManager`
- [ ] Обновить импорты

**Обновить main.py:**
- [ ] Убедиться, что передаваемые объекты соответствуют Protocol
- [ ] Код должен работать без изменений (duck typing)

**Обновить тесты:**
- [ ] В test_bot.py использовать моки с Protocol типами
- [ ] Убедиться, что тесты проходят после рефакторинга

**Проверка соответствия:**
- [ ] ✅ Минимум новых абстракций - только для ключевых зависимостей (`conventions.mdc`)
- [ ] ✅ Protocol проще, чем ABC - выбираем Protocol (`conventions.mdc`)
- [ ] ✅ Не создаем избыточных уровней абстракции (`conventions.mdc`)
- [ ] ✅ Код остается простым и понятным (`vision.md`)
- [ ] ✅ Легко добавить новые имплементации в будущем (SOLID DIP)

**Тест:** Запустить `make quality` - все проверки проходят, тесты работают

---

### Итерация TD-5: Рефакторинг: TelegramBot - Single Responsibility
**Цель:** Разделить TelegramBot на более мелкие компоненты (SOLID SRP)

**Анализ текущих обязанностей:**
- [ ] TelegramBot содержит: инфраструктуру aiogram + обработку команд + бизнес-логику
- [ ] Необходимо разделить на: TelegramBot (инфраструктура) + MessageHandler (логика)

**Создать src/bot/message_handler.py:**
- [ ] Создать класс `MessageHandler` - бизнес-логика обработки сообщений
- [ ] Перенести метод `handle_message` из `TelegramBot`
- [ ] Конструктор принимает `LLMProvider` и `DialogueStorage`
- [ ] Метод `async def handle_user_message(user_id: int, username: str, text: str) -> str`
- [ ] Возвращает текст ответа вместо прямой отправки
- [ ] Добавить полные type hints

**Создать src/bot/command_handler.py:**
- [ ] Создать класс `CommandHandler` - обработка команд бота
- [ ] Перенести команды `/start`, `/help`, `/reset` из `TelegramBot`
- [ ] Конструктор принимает `DialogueStorage`
- [ ] Методы возвращают текст ответа: `get_start_message() -> str`
- [ ] Метод `reset_dialogue(user_id: int) -> str`
- [ ] Добавить полные type hints

**Обновить src/bot/bot.py:**
- [ ] Упростить `TelegramBot` - только инфраструктура aiogram
- [ ] Конструктор принимает `MessageHandler` и `CommandHandler`
- [ ] Команды делегируют работу в `CommandHandler` и отправляют ответ
- [ ] `handle_message` делегирует в `MessageHandler` и отправляет ответ
- [ ] Убрать всю бизнес-логику из класса

**Обновить src/bot/main.py:**
- [ ] Создать экземпляры `MessageHandler` и `CommandHandler`
- [ ] Передать их в конструктор `TelegramBot`
- [ ] Убедиться, что всё работает корректно

**Создать тесты:**
- [ ] `tests/test_message_handler.py` - тесты для MessageHandler
- [ ] `tests/test_command_handler.py` - тесты для CommandHandler
- [ ] Обновить `tests/test_bot.py` - теперь тестируем только инфраструктуру

**Проверка соответствия:**
- [ ] ✅ Каждый класс имеет одну ответственность (SOLID SRP)
- [ ] ✅ Код проще для понимания и тестирования
- [ ] ✅ Следуем принципу "1 класс = 1 файл" (`conventions.mdc`)
- [ ] ✅ Не создали избыточных абстракций - только логическое разделение
- [ ] ✅ Модульная структура соответствует `vision.md`

**Финальная проверка:**
- [ ] Запустить `make quality` - все проверки проходят
- [ ] Покрытие тестами не снизилось (≥80%)
- [ ] Код легче читать и тестировать
- [ ] Легко добавить новые команды или типы сообщений

**Тест:** Запустить бота вручную, протестировать все команды и текстовые сообщения

---

## 🎯 Критерии завершения Tech Debt итераций

**После TD-1 (Автоматизация):**
- ✅ Все инструменты настроены в `pyproject.toml`
- ✅ Makefile содержит команды `format`, `lint`, `typecheck`, `test`, `quality`
- ✅ `make quality` выполняется без критических ошибок

**После TD-2 (Type hints):**
- ✅ Все модули имеют полные type hints
- ✅ `mypy --strict` проходит без ошибок
- ✅ Код понятен благодаря типам

**После TD-3 (Тесты):**
- ✅ Покрытие тестами ≥80%
- ✅ Все основные компоненты покрыты тестами
- ✅ `tests/conftest.py` содержит переиспользуемые фикстуры

**После TD-4 (Protocol/DIP):**
- ✅ Создан `interfaces.py` с Protocol классами
- ✅ TelegramBot зависит от абстракций, а не конкретных классов
- ✅ Легко добавить альтернативные имплементации

**После TD-5 (SRP):**
- ✅ TelegramBot - только инфраструктура
- ✅ MessageHandler - бизнес-логика сообщений
- ✅ CommandHandler - обработка команд
- ✅ Каждый класс имеет одну четкую ответственность

---

## 📝 Правила работы с Tech Debt

**Общие правила:**
- После каждой итерации обновляй таблицу прогресса
- Запускай `make quality` после каждого изменения
- Все изменения должны быть покрыты тестами
- Следуй всем соглашениям из `conventions.mdc`
- Не отклоняйся от простоты и принципа KISS
- Коммить после успешного прохождения всех проверок

**Проверка соответствия документам на каждой итерации:**
- [ ] ✅ Соответствует `conventions.mdc` (KISS, без оверинжиниринга)
- [ ] ✅ Соответствует `workflow.mdc` (согласование → реализация → проверка)
- [ ] ✅ Соответствует `vision.md` (MVP подход, простота, ООП с 1 класс = 1 файл)
- [ ] ✅ Следует принципам SOLID (SRP, DIP) без избыточности

**Последовательность выполнения:**
- TD-1 → TD-2 → TD-3 можно выполнять последовательно или параллельно
- TD-4 → TD-5 выполняются только после завершения TD-1,2,3
- Не переходи к следующей итерации без завершения предыдущей

**Приоритет:**
1. **Критично**: TD-1 (инструменты), TD-2 (типы)
2. **Важно**: TD-3 (тесты)
3. **Желательно**: TD-4 (DIP), TD-5 (SRP)

---

## 🔄 Интеграция с основным tasklist.md

**Связь с основной разработкой:**
- Tech Debt итерации выполняются параллельно с функциональными итерациями
- Рекомендуется выполнить TD-1 и TD-2 перед итерацией 6 (HomeGuru)
- TD-3 можно выполнять инкрементально вместе с новыми фичами
- TD-4 и TD-5 - опционально, после завершения основного MVP

**Момент для выполнения:**
- **Сейчас**: TD-1, TD-2 (после итерации 5 - Логирование)
- **Перед итерацией 6**: TD-3 (расширение тестов)
- **После MVP**: TD-4, TD-5 (рефакторинг архитектуры)

---

## 💡 Заметки

**Важно помнить:**
- Качество кода важно, но не важнее работающего MVP
- Все улучшения должны сохранять простоту проекта
- Type hints помогают, но не должны усложнять код
- Тесты нужны для уверенности, а не для метрик
- Рефакторинг делаем только когда есть реальная боль
- SOLID применяем разумно, без фанатизма

**Когда делать Tech Debt:**
- ✅ После завершения базовой функциональности (итерации 1-5)
- ✅ Перед добавлением сложных фич (итерации 6-9)
- ✅ Когда код становится сложно поддерживать
- ❌ Не делаем "на будущее" без реальной необходимости

**Best Practices для проекта:**
- Используем Ruff вместо Black + Flake8 (проще и быстрее)
- Используем Protocol вместо ABC (проще и pythonic)
- Coverage ≥80% - разумный баланс между качеством и временем
- Strict mypy - помогает найти баги на ранней стадии
- Make для автоматизации - просто и эффективно

---

**Последнее обновление:** 2025-10-11

